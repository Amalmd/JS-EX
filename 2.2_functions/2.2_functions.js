function tellFortune(jobTitle, location, partnerName, numberOfChildren) {
    return `You will be a ${jobTitle} in the ${location} and married to ${partnerName} with ${numberOfChildren} children.`;
  }

  const fortune = tellFortune('Programer', 'UK', 'Omar', 3);